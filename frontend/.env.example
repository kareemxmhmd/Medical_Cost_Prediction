# FastAPI Backend URL (accessed from browser or client)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional API Key for protected endpoints (defaults to dev key if not provided)
NEXT_PUBLIC_API_KEY=dev-api-key-change-in-production
